package BinarySearch

object Search2DMatrix {
    fun searchMatrix(matrix: Array<IntArray>, target: Int): Boolean {
        return  true
    }
    fun main() {
        searchMatrix(arrayOf(intArrayOf(1,3,5,7), intArrayOf(10,11,16,20), intArrayOf(10,11,16,20)), 3)
    }
}